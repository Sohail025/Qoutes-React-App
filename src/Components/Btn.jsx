export default function Btn({ children }) {
  return (
    <div className="h-auto py-2 px-2 w-[6.7rem] bg-[#1c585f] text-center rounded-[0.7rem] shadow-lg font-semibold text-white text-[0.8rem] text-nowrap">
      {children}
    </div>
  );
}
